<Page
    x:Class="UWP.CueLMS.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:UWP.CueLMS"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height=" 11*"/>
        </Grid.RowDefinitions>
        <Grid Background="IndianRed">
            <TextBlock Text="Home" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" Grid.Row="0" FontSize="50"/>
        </Grid>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="10*"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="30">
                <Button VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="Instructor View" FontSize="50" Click="InstructorView_Click"/>
                <Button VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Content="Student View" FontSize="50"/>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
